$authorList.on('click', function() {
  $authorList.next()
}
$(function)() {
  var authButton;
  $('option').on('click',':not(#....)',
  function(pushButton) {
  authorList = 'Item: ' pushButton.target.textContent + '<br />';
  $('#authorButton').html(this.authorList)
  };

$authButton.show();
$authButton.hide();

$('#authButton').on('click',function() {
  $authButton.show();
  $authButton.hide();
});

   <
$(function () {
  $('#aboutMe').hide();
}
$('article a').on('click', function() {
    event.preventDefault();
    $(this).prev().children('.hide').toggle();
  });
//pull authorList
Article.prototype.toHTML = function() {
  var $authorList = $('#authButton').clone();
//click feature
  var $ = $('click:text').val();

    //console.log($article);
};


$('#categories').change(function() {
  var $clickedAuth = $('#author').find(':selected').text();
  $('article').hide();
  $('.'$clickedAuth).show();
});


$article.on('authButton', 'option', function() {
  var $authorTab = $(authorTab);
//filter list
  var complete = $this.hasClass('complete');
    if (complete === true) {
      $this.show()
    };
  } else {
    item = $this.text();
  }
}
//show category list in dropdown
Article.prototype.toHTML = function() {
  var $article = $('#catButton').clone();
  $article.removeAttr('id');
  $article.find('.category').html(this.category);

$article.on('catButton', 'option', function() {
  var $authorTab = $(authorTab);

//filter list
  var complete = $this.hasClass('complete');
    if (complete === true) {
      $this.show()
    };
  } else {
    item = $this.text();
  }
}
//select authButton + hide list
$(function() {
var authButton;
$('authButton').click(pushButton {
  authorList = 'Item: ' pushButton.target.textContent + '<br />';
  $('#authorButton').html(this.authorList)
    };
})

  //select catButton + hide list
  $(function() {
  var authButton;
  $('option').on(
  'click mouseover',
  ':not(#....)',
    function(pushButton) {
    authorList = 'Item: ' pushButton.target.textContent + '<br />';
    $('#authorButton').html(this.authorList)
    };
        //console.log($article);
    };
